name: socket_test
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0-153.0.dev <4.0.0'

dependencies:
  collection: ^1.18.0
  dartz: ^0.10.1
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^13.2.4
  http: ^1.2.1
  shared_preferences: ^2.2.3
  slide_to_act: ^2.0.2
  validatorless: ^1.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
